import { SearchDescriptionPipe } from './search-description.pipe';

describe('SearchDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
